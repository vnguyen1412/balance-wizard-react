export const isAdmin = () => {
    // Logic to determine if the current user is an admin
    // This could involve checking user roles or permissions
    // For now, let's assume it always returns true for simplicity
    return true;
};